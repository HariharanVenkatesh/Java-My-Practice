server:
  port: 8080

spring:
  application:
    name: Weather-App

  datasource:
    url: 'jdbc:postgresql://localhost:5432/Weather'
    username : postgres
    password : Hanis@2323

    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect

weather:
  api:
    key: 'weather.api.key'